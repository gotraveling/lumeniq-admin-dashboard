/** /console/browse — hotel inventory browser inside the console shell.
 *  Reuses the legacy /admin/inventory/browse component. */
import BrowseHotels from '../../admin/inventory/browse/page';

export default function ConsoleBrowsePage() {
  return <BrowseHotels />;
}
