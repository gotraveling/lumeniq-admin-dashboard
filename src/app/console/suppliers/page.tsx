/** /console/suppliers — supplier status/config inside the console shell.
 *  Reuses the legacy /admin page component. */
import SuppliersPage from '../../admin/suppliers/page';

export default function ConsoleSuppliersPage() {
  return <SuppliersPage />;
}
